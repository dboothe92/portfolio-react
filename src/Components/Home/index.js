import React from 'react';

const Home = () => {
    return(
        <section className="text-center landing-text">
            Welcome, <br/>
            let's create something!
        </section>
    );
};

export default Home;