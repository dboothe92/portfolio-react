import React from 'react';

const Resume = () => {
    return(
        <section>
            <p>
                This is where my resume will be displayed!
            </p>
        </section>
    );
};

export default Resume;