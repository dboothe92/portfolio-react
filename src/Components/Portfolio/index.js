import React from 'react';

const Portfolio = () => {
    return(
        <section>
            <h1 id="portfolio">
                Portfolio
            </h1>
        </section>
    );
};

export default Portfolio;